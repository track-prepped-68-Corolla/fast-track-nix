[package]
name = "mullet"
version = "0.1.0"
edition = "2021"
description = "Standalone Rust replica of scripts/mullet.just — experiment, not wired into the ft CLI."

[[bin]]
name = "mullet"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
